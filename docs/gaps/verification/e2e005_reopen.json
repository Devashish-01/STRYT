[{"gap_id": "E2E-005", "status": "FIXED", "evidence": "Reopened 2026-09-16: the earlier closure was wrong (the breadth spec checked for the toast once; it appears ~0.6-1.4s later). Root cause proven: bulkService sweepExpiredDeals called .catch() on the Supabase rpc builder (a thenable without catch) -> TypeError rejected every deal read in the sweep window -> Couldn't load on bulk-deals console and Community hub (6/6 fresh loads). Fix: await the builder inside try/catch; unit test bulkService.sweep.test.ts fails without the fix (TypeError) and passes with it. screens.spec now records any error toast during load (MutationObserver) and fails on the unfixed build for both screens. Final proof: next full E2E run on the rebuilt app.", "notes": "src/services/marketplace/bulkService.ts; scan found no other .catch on query builders."}]
