---
inclusion: manual
---

Follow `AGENTS-FEATURE-MAP.md` at the agent kit root. Run
`scripts/generate-feature-file-map.py` then enrich maps per feature.
Write output to `new-tracker/<project-slug>/file-maps/`.
