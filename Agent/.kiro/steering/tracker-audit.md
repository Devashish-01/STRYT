---
inclusion: manual
name: tracker-audit
description: Load tracker audit agent instructions when running a spec-vs-code audit
---

# Tracker audit (pointer)

Do not duplicate workflow logic here. When the user says **"Run tracker
audit"**, **"resume"**, **"re-discover"**, or **"start fresh"**:

1. Read `AGENTS.md` at the agent kit root in full.
2. Follow `discovery-rules.md`, `review-rubric.md`, `new-tracker-template.md`.
3. Use invoke summary in `wrappers/codebase-reviewer.body.md`.

Or select the **tracker-audit** custom agent in `.kiro/agents/tracker-audit.md`.
