node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
*.log
.vite

# Playwright audit
.auth
test-results
playwright-report
playwright/.cache

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Editor / OS
Thumbs.db
*.swp

# Capacitor (native wrappers — committed so CI can build Android APK / iOS archive)
# Only ignore build outputs and local secrets
android/.gradle
android/app/build
android/build
android/local.properties
android/app/stryt-release.keystore
android/.idea
# iOS build/output ignores live in ios/.gitignore (Pods, DerivedData, public web copy)
.capacitor

# APK binary — served from GitHub Releases, not stored in git
public/stryt.apk


# Firebase service account private keys — NEVER commit these
*firebase-adminsdk*.json
google-services.json
android/app/google-services.json

/skills
.agents
.claude
.vercel
.env*
